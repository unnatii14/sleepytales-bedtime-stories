[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Ensure Vite's base is set to the site root during Netlify builds
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
